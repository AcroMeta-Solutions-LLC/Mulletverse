export type ThemeType = {
  PRIMARY: string;
  BACKGROUND: string;
  NAVIGATION: string;
  TEXT: string;
  TITLE: string;
  BORDER: string;
  INPUT: string;
  CARD: string;
};

export default ThemeType;
